import 'package:flutter/material.dart';

class ScreenTeam extends StatelessWidget {
  const ScreenTeam({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Team details page'),
      ),
    );
  }
}