import 'package:flutter/material.dart';

class ScreenProjectDetails extends StatelessWidget {
  const ScreenProjectDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Project details page'),
      ),
    );
  }
}