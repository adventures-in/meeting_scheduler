name: meeting_scheduler
description: Adventures in scheduling across timezones.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=2.15.0-116.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^1.10.6
  firebase_messaging: ^11.2.4
  go_router: ^2.5.5
  flutter_riverpod: ^1.0.3
  flutterfire_ui: ^0.3.0
  google_sign_in: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
